services:
  vida:
    build: .
    container_name: vida-platform
    restart: unless-stopped
    ports:
      - "3001:3000"
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - TZ=America/Bogota
      - SANDRA_WEB_DIR=/app/sandra-web
    mem_limit: 700m
    healthcheck:
      test: ["CMD", "node", "-e", "require('http').get('http://localhost:3000/api/health', (r) => { let d=''; r.on('data',c=>d+=c); r.on('end',()=>{const j=JSON.parse(d);if(j.status!=='ok'||!j.db)throw new Error(j.status)}) })"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
